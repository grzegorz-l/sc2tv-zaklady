<p id="notice"><%= notice %></p>
<h1>Newsy</h1>

<table>
  <tr>
    <th>Tytuł</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @posts.each do |post| %>
  <tr>
    <td><%= post.title %></td>
    <td><%= link_to 'Pokaż', post %></td>
    <td><%= link_to 'Edytuj', edit_post_path(post) %></td>
    <td><%= link_to 'Usuń', post, :confirm => 'Jesteś pewien?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Dodaj news', new_post_path %>
